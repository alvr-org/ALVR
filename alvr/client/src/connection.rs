use alvr_common::{logging::show_err, sockets::*, *};



pub async fn connection_loop() {
    loop {
        // show_err(|| -> StrResult {
        //     let control_socket = ControlSocket::connect_to_server(headset_info, hostname, certificate_pem)

        //     Ok(())
        // }())
        // .ok();
    }
}
