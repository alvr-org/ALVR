import React from "react"
import { SchemaSection, SessionSettingsSection } from "../../sessionManager"

export function Section({
    schema,
    session,
}: {
    schema: SchemaSection
    session: SessionSettingsSection
}): JSX.Element {
    return <></>
}
