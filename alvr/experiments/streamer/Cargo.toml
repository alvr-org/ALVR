[package]
name = "alvr_streamer"
version = "0.1.0"
authors = ["alvr-org"]
license = "MIT"
edition = "2021"
rust-version = "1.58"

[lib]
crate-type = ["cdylib"]

[dependencies]
alvr_common = { path = "../../common" }
alvr_session = { path = "../../session" }
